@AGENTS.md

# 협업 규칙 (space-joops / joop-03)

- **모든 소통은 한국어로 진행합니다.** 커밋 메시지, PR 설명, 코드 리뷰 코멘트 등도 가능하면 한국어를 우선합니다.
- **의미 있는 작업(인프라 변경, 주요 기능, 트러블슈팅 등)을 마치면 `docs/worklog/`에 날짜별 작업 로그를 남깁니다.** 목적은 나중에 합류하는 사람이 "무엇을, 왜, 어떻게" 했는지 읽고 배울 수 있게 하는 것입니다. 형식: `docs/worklog/YYYY-MM-DD-제목.md`. 문제 상황 → 선택지 → 왜 그렇게 결정했는지를 친절하게 풀어서 씁니다.
- **인프라 현황(GitHub/Vercel/Supabase 연동, 제약사항, 환경변수)은 `docs/infra.md`에서 관리합니다.** 인프라가 바뀌면 이 문서도 함께 갱신하세요.
- **작업 방식은 PR 기반입니다.** main에 바로 커밋하지 않고, 브랜치를 만들어 PR로 올린 뒤 병합합니다.
